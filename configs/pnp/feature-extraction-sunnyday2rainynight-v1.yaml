config:
  experiment_name: "sunnyday2rainynight-v1"
  init_img: "This one won't be used, as we are using the images from one folder."
  ddim_inversion_steps: 100 # better use 999 steps for the best reconstruction
  ddim_steps: 100
  save_feature_timesteps: 50 # save 50 features, interval = ddim_steps/50